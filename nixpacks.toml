[phases.setup]
nixPkgs = ["python311"]

[phases.install]
cmds = ["cd app && python -m pip install -r requirements.txt"]

[start]
cmd = "cd app && python -m uvicorn main:app --host 0.0.0.0 --port $PORT"
